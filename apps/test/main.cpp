#include <boost/lambda/lambda.hpp>
#include <iostream>
#include <iterator>
#include <algorithm>

#include <network/tcp_connection.hpp>

int main()
{
    IRPM::tcp_print();
    IRPM::testasio();
    exit(0);
}